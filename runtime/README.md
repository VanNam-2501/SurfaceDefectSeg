# Runtime binaries

`wheels/vmamba/` chứa wheel Mamba được build cho đúng phiên bản Python, PyTorch,
CUDA và compute capability. Wheel là binary lớn, không commit vào Git; phải ghi
rõ môi trường tương thích trong hướng dẫn setup.

